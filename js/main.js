// main.js — общий входной модуль, импортирует и запускает общие UI-модули
// Глобальные стили импортируем явно, чтобы Vite включил их в сборку
import '/styles.css';
import './menu.js';
import './lightbox.js';

// экспорт пустой объект для явного ESM-модуля (необязательно)
export {};
